<p>
  <%= machine.name %>,
  <%= machine.mac %>,
  <%= machine.ip.ip %>
  <%= link_to "edit", edit_machine_path(machine) %>
  <%= button_to "delete", machine, method: :delete, data: {turbo_confirm: "Are you sure ?"} %>
</p>
